
//Question 1: Write a C# program that prints your name, age, and favorite hobby on separate lines.
#region
/*
Console.WriteLine("My name is Mohamed H. Fawzi");
Console.WriteLine("I've 20 years-old");
Console.WriteLine("My Favorite hobby is watch movies");
*/
#endregion


//Question 2: Write a C# program that declares variables of different data types (int, double, string, bool) and assigns them values. Then, print these values to the console.
#region
/*
int age = 20;
double gpa = 4.2;
String name = "Mohamed";
bool q = true;

Console.WriteLine("Your name is " + name + " ,You've " + age + " years-old");
Console.WriteLine("Your GPA is " + gpa + " ,You are login in University " + q);
*/
#endregion


//Question 3: Write a C# program that converts a temperature value from Celsius to Fahrenheit. The Celsius temperature should be stored in a variable.
#region
/*
double celsius = 32.0;
double fahrenheit = (celsius * 9 / 5) + 32;

Console.WriteLine("Temerature in celsius is " + celsius + " To fahrenheit is " + fahrenheit);
*/
#endregion


//Question 4: Write a C# program that takes two numbers as input and performs the following operations: addition, subtraction, multiplication, and division. Print the results of each operation.
#region
/*
class MathOperations
{
    // Addition Method 
    static int Add(int fnum, int snum)
    {
        return fnum + snum;
    }

    // Subtraction Method 
    static int Subtract(int fnum, int snum)
    {
        return fnum - snum;
    }

    // Multipulation Method 
    static int Multiply(int fnum, int snum)
    {
        return fnum * snum;
    }

    // Dividtion Method 
    static double Divide(int fnum, int snum)
    {
        if (snum == 0)
        {
            Console.WriteLine("Math Logic ERROR! Division by zero.");
            return double.NaN;
        }
        else
        {
            return (double)fnum / snum;
        }
    }

    static void Main()
    {
        // Input first number and Convert it to integer type 
        Console.Write("Enter First number: ");
        int fnum = Convert.ToInt32(Console.ReadLine());


        // Input second number and Convert it to integer type
        Console.Write("Enter Second number: ");
        int snum = Convert.ToInt32(Console.ReadLine());

        Console.WriteLine("Result of Addition is:" + Add(fnum,snum));
        Console.WriteLine("Result of Subtraction is:" + Subtract(fnum, snum));
        Console.WriteLine("Result of Multiply is:" + Multiply(fnum, snum));
        Console.WriteLine("Result of Divide is:" + Divide(fnum, snum));

        Console.Read();
    }
}
*/
#endregion


//Question 5: Write a C# program that calculates the area of a rectangle. The program should prompt the user to enter the length and width of the rectangle, and then calculate and display the area.
#region

/*
class RectangleArea
{
    // To calculate Area of Rectangle
    static double rectangleArea(double Length, double Width)
    {
        return Length * Width;
    }

    static void Main()
    {
        Console.Write("Enter length number: ");
        int Length = Convert.ToInt32(Console.ReadLine());

        Console.Write("Enter width number: ");
        int Width = Convert.ToInt32(Console.ReadLine());

        Console.WriteLine("Area of rectangle is " + rectangleArea(Length,Width));
        Console.Read();
    }
}
*/
#endregion


//Question 6: Write a C# program that takes an integer as input and checks if it is even or odd. Print a message indicating whether the number is even or odd.
#region
/*
class EvenOrOod
{
    static void Main()
    {
        Console.Write("Enter your Integer number: ");
        int num = Convert.ToInt32(Console.ReadLine());

        if (num % 2 == 0)
        {
            Console.WriteLine(num + " is an Even Number");
        }

        else if (num % 2 != 0)
        {
            Console.WriteLine(num + " is an Even Number");
        }
        else
        {
            Console.WriteLine(num + " is equal Zero");
        }
        Console.ReadLine();
    }
}
*/
#endregion


//Question 7: Write a C# program that prompts the user to enter their age. If the age is 18 or older, print "You are eligible to vote." If the age is less than 18, print "You are not eligible to vote."
#region
/*
class age
{
    static void Main()
    {
        Console.Write("Enter Your Age: ");
        int age = Convert.ToInt32(Console.ReadLine());

        if (age >= 18)
        {
            Console.WriteLine("You are eligible to vote.");
        }
        else
        {
            Console.WriteLine("You are not eligible to vote.");
        }
        Console.Read();
    }
}
*/
#endregion


//Question 8: Write a C# program that simulates a simple grading system. The program should prompt the user to enter a score (0-100), and then print the corresponding grade based on the following criteria:
//90-100: A
//80-89: B
//70-79: C
//60-69: D
//Below 60: F
#region
/*
class GradingSystem
{
    static void Main()
    {
        Console.Write("Enter Your Score: ");
        int score = Convert.ToInt32(Console.ReadLine());

        if (score >= 90 && score <= 100)
        {
            Console.WriteLine("A");
        }
        else if (score >= 80 && score <= 89)
        {
            Console.WriteLine("B");
        }
        else if (score >= 70 && score <= 79)
        {
            Console.WriteLine("C");
        }
        else if (score >= 60 && score <= 69)
        {
            Console.WriteLine("D");
        }
        else if (score >= 0 && score < 60)
        {
            Console.WriteLine("F");
        }
        else
        {
            Console.WriteLine("Your Score is Invalid");
        }
        Console.Read();
    }
}
*/
#endregion


//Question 8: Write a C# program that simulates a simple grading system. The program should prompt the user to enter a score (0-100), and then print the corresponding grade based on the following criteria:
//90-100: A
//80-89: B
//70-79: C
//60-69: D
//Below 60: F

#region
/*
class gradingSystem
{
    static void Main()
    {
        Console.Write("Enter Your Letter: ");
        char GPA = Convert.ToChar(Console.ReadLine().ToUpper());

        switch (GPA)
        {
            case 'A':
                Console.WriteLine("Your GPA is  greater than 4.2");
                break;

            case 'B':
                Console.WriteLine("Your GPA is  greater than 3.9");
                break;

            case 'C':
                Console.WriteLine("Your GPA is greater than 3.2");
                break;

            case 'D':
                Console.WriteLine("Your GPA is Less than 2.8");
                break;

            case 'F':
                Console.WriteLine("Your GPA is Failed");
                break;
        }
    }
}
*/
#endregion